# node
node_modules
.pnpm-store

# next
.next
out
*.tsbuildinfo
next-env.d.ts

# tauri / rust
src-tauri/target
src-tauri/gen

# os
.DS_Store
.idea
.vscode

# cetus additions
*.log

# pi install tree (~260 MB, platform-specific). Rebuild via
# scripts/build-pi-sidecar.sh on each developer machine.
src-tauri/pi-install/
# Legacy locations from the previous sidecar approach.
src-tauri/binaries/

# In-repo clones used only for code reading (e.g. deepseek-tui).
research/

# Local agent / skill tooling (Claude Code, etc.) — not part of the app
.claude/
.agents/
*.original.backup.*

# Scratch image-generation outputs
output/

# Local secrets / env
.env
.env.local
.env*.local

# Local signing config (identity string is private; never commit)
scripts/signing.env
scripts/release.env

# Eval answer keys / de-blinding maps — kept local, NOT shipped publicly
# (avoids benchmark contamination once the repo is public).
evals/general-benchmark/v2/fixtures/**/expected.json
evals/web-search/mapping.json
evals/web-search/results.json

# Cetus app runtime state created when running Cetus inside this repo.
.cetus/
