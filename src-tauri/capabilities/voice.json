{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "voice",
  "description": "Minimal capability for the global dictation HUD window: listens for voice events, dismisses itself, and offers explicit copying of a deferred dictation result.",
  "windows": ["voice"],
  "permissions": ["core:default"]
}
