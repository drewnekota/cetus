{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "quick",
  "description": "Minimal capability for the always-summonable frameless launcher window: invoke the quick_* commands and listen for events, nothing else (no dialog / notification surface).",
  "windows": ["quick"],
  "permissions": ["core:default"]
}
