{
  "name": "cetus-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/icons.mjs && esbuild src/main.js --bundle --minify --format=esm --outfile=main.js",
    "dev": "python3 -m http.server 4178 --bind 127.0.0.1 --directory .",
    "check": "node --check src/main.js && node --check main.js && node scripts/check.mjs",
    "deploy": "npm run build && npm run check && node scripts/stage.mjs && wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "lucide": "^0.469.0"
  },
  "dependencies": {
    "@paper-design/shaders": "^0.0.80"
  }
}
